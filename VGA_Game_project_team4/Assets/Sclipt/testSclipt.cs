using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testSclipt : MonoBehaviour
{
    int test = 100;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(test);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
